# Shared config for the app services (shared/api/web).
x-app: &app
  build:
    context: .
    dockerfile: Dockerfile.dev
  # Bind-mount the whole repo for hot reload; mask every node_modules with
  # anonymous volumes so the container uses the Linux deps baked into the
  # image rather than the host's (macOS) ones.
  volumes:
    - .:/app
    - /app/node_modules
    - /app/packages/shared/node_modules
    - /app/packages/api/node_modules
    - /app/packages/web/node_modules
  environment: &app-env # File watching over bind mounts on macOS/Windows needs polling.
    CHOKIDAR_USEPOLLING: 'true'

services:
  postgres:
    image: postgres:17-alpine
    container_name: jasamkrompir-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: jasamkrompir
      POSTGRES_PASSWORD: jasamkrompir
      POSTGRES_DB: jasamkrompir
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U jasamkrompir -d jasamkrompir']
      interval: 5s
      timeout: 5s
      retries: 5

  # Compiles @jasamkrompir/shared to dist and watches for changes.
  shared:
    <<: *app
    container_name: jasamkrompir-shared
    command: yarn workspace @jasamkrompir/shared dev
    healthcheck:
      test: ['CMD', 'test', '-f', '/app/packages/shared/dist/index.js']
      interval: 2s
      timeout: 2s
      retries: 30

  migrate:
    <<: *app
    container_name: jasamkrompir-migrate
    command: yarn workspace @jasamkrompir/api db:migrate
    environment:
      <<: *app-env
      DATABASE_URL: postgres://jasamkrompir:jasamkrompir@postgres:5432/jasamkrompir
    depends_on:
      postgres:
        condition: service_healthy
      shared:
        condition: service_healthy

  api:
    <<: *app
    container_name: jasamkrompir-api
    command: yarn workspace @jasamkrompir/api dev
    environment:
      <<: *app-env
      DATABASE_URL: postgres://jasamkrompir:jasamkrompir@postgres:5432/jasamkrompir
      PORT: '3000'
      HOST: '0.0.0.0'
      NODE_ENV: development
    ports:
      - '3000:3000'
    depends_on:
      postgres:
        condition: service_healthy
      shared:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  web:
    <<: *app
    container_name: jasamkrompir-web
    command: yarn workspace @jasamkrompir/web dev --host
    environment:
      <<: *app-env
      # Browser runs on the host, so it reaches the API via the mapped port.
      VITE_API_URL: http://localhost:3000
    ports:
      - '5173:5173'
    depends_on:
      shared:
        condition: service_healthy

volumes:
  postgres-data:
